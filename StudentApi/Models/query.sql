// create database e_school_students
      /*
        CREATE TABLE Students (
            id int IDENTITY(1,1) PRIMARY KEY,
            nome varchar(150) NOT NULL,
            matricula varchar(15) NOT NULL,
            notas varchar(255)
        );
      */